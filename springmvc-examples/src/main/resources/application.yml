#自动读取生成数据表
spring:
  jackson:
    default-property-inclusion: non_null
logging:
  level:
    sql: debug
    org:
      example: debug
    root: warn
  pattern:
    console: '%-5level %C.%M[%line] - %msg%n'


my:
  secretkey: OIioun9